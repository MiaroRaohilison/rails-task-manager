<h2><%= @task.title %></h2>
<div>
  <p><%= @task.details %></p>
</div>

<p><input type="checkbox">This task is <%= "not" if @task.completed == false %> completed</p>

<%= link_to "Back to list", tasks_path %>

<%= link_to "Edit task", edit_task_path %>
